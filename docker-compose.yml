services:
  frontend-impact-center:
    image: node:22-alpine
    working_dir: /workspace/frontend-impact-center
    volumes:
      - ./:/workspace
    command: sh -c "npm install && npm run dev"
    ports:
      - "4200:4200"
    depends_on:
      - auth-service
      - experience-gateway
      - contract-analyzer-service
      - mock-core-system
      - agent-tools-service

  auth-service:
    image: eclipse-temurin:21-jdk
    working_dir: /workspace/auth-service
    volumes:
      - ./:/workspace
    command: sh -c "./mvnw spring-boot:run"
    ports:
      - "8081:8080"

  experience-gateway:
    image: eclipse-temurin:21-jdk
    working_dir: /workspace/experience-gateway
    volumes:
      - ./:/workspace
    command: sh -c "./mvnw spring-boot:run"
    ports:
      - "8082:8080"

  contract-analyzer-service:
    image: eclipse-temurin:21-jdk
    working_dir: /workspace/contract-analyzer-service
    volumes:
      - ./:/workspace
    command: sh -c "./mvnw spring-boot:run"
    ports:
      - "8083:8080"

  mock-core-system:
    image: eclipse-temurin:21-jdk
    working_dir: /workspace/mock-core-system
    volumes:
      - ./:/workspace
    command: sh -c "./mvnw spring-boot:run"
    ports:
      - "8084:8080"

  agent-tools-service:
    image: eclipse-temurin:21-jdk
    working_dir: /workspace/agent-tools-service
    volumes:
      - ./:/workspace
    command: sh -c "./mvnw spring-boot:run"
    ports:
      - "8085:8080"
