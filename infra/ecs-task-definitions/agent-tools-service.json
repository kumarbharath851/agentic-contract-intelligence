{
  "family": "aci-agent-tools",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",
  "executionRoleArn": "arn:aws:iam::866543941893:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::866543941893:role/ecsTaskRole",
  "containerDefinitions": [
    {
      "name": "agent-tools-service",
      "image": "866543941893.dkr.ecr.us-east-1.amazonaws.com/aci-agent-tools-service:latest",
      "portMappings": [{ "containerPort": 8085, "protocol": "tcp" }],
      "essential": true,
      "environment": [
        { "name": "SERVER_PORT", "value": "8085" },
        { "name": "SPRING_PROFILES_ACTIVE", "value": "prod" },
        { "name": "ANALYZER_URL", "value": "http://aci-contract-analyzer:8083" }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/aci-agent-tools",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "healthCheck": {
        "command": ["CMD-SHELL", "curl -f http://localhost:8085/health || exit 1"],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 60
      }
    }
  ]
}
